services:
  - type: web
    name: MedicalRiskAPI
    env: python
    region: oregon # ou ta région
    plan: free
    branch: main
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: python MedicalRiskapi.py
    pythonVersion: "3.12.7"
